import { Box } from "@chakra-ui/react";
import HeroSection from "../layout/HeroSection";

const HomePage = () => {
  return (
    <Box>
      <HeroSection />
    </Box>
  );
};

export default HomePage;
